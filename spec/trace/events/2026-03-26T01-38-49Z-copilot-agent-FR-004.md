---
req_id: FR-004
change_type: implement
files:
  [
    src/server.ts,
    test/server.test.ts,
    spec/requirements/FR-004.md,
    spec/requirements/FR-008.md,
    spec/TRACE.md,
  ]
tests: [pnpm typecheck, pnpm test, pnpm spec:validate]
docs: [spec/requirements/FR-004.md, spec/requirements/FR-008.md, spec/TRACE.md]
pr: null
commit: null
author: copilot-agent
timestamp: 2026-03-26T01:38:49Z
---

Hardened rate-limiter state management, aligned URL policy spec, and added targeted tests
for worker-failure and client-cancellation paths.

Changes:

- SlidingWindowRateLimiter: added periodic stale-bucket sweep (setInterval, unref) to bound
  Map growth, added destroy() method, wired destroy() to server close.
- RenderWorkerPool: exported class, added terminateWorker(index) for test-driven crash
  simulation, updated createApiServer to attach pool on the returned ApiServer object.
- ApiServer type exported (Server & { pool: RenderWorkerPool }).
- test/server.test.ts: four new tests — http:// font URL → 422, idle worker crash + recovery,
  worker crash during render → 500 + queued task recovered → 200, client disconnect → recovery.
- FR-004 acceptance criteria extended with worker-crash recovery guarantees.
- FR-008 acceptance criteria clarified: ALL non-HTTPS font URL schemes rejected, not just
  unsafe ones (http:// explicitly called out).
- spec/TRACE.md notes updated for FR-004 and FR-008.
