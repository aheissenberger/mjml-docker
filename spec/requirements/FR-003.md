---
id: FR-003
type: FR
status: Done
owner: copilot-agent
depends_on: [FR-001]
acceptance_refs: []
implementation_pointers: [src/server.ts]
parent_req_id: null
package_scope: null
---

## Summary

The server must expose a root info endpoint at `GET /` that returns server name,
a descriptive message, and the list of available API endpoints as JSON.

## Acceptance Criteria

- `GET /` returns HTTP 200
- Response body includes `name`, `message`, and `endpoints` fields (JSON)
- `endpoints` lists all available routes

## Verification

- Automated: `test/server.test.ts` — "GET / returns name, message and endpoints fields"
- Smoke tested: `curl http://localhost:3000/` returns `{"name":"...","message":"...","endpoints":[...]}`

## Notes

No auth required for `GET /health`; `GET /` is auth-protected per FR-005.
