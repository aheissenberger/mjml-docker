---
id: FR-002
type: FR
status: Done
owner: copilot-agent
depends_on: [FR-001]
acceptance_refs: []
implementation_pointers: [src/server.ts]
parent_req_id: null
package_scope: null
---

## Summary

The server must expose a health check endpoint at `GET /health` that returns
`{ "status": "ok" }` with HTTP status 200.

## Acceptance Criteria

- `GET /health` returns HTTP 200
- Response body is `{ "status": "ok" }` (JSON)
- Response includes `content-type: application/json` header

## Verification

- Automated: `test/server.test.ts` — "GET /health returns ok"
- Smoke tested: `curl http://localhost:3000/health`

## Notes

Standard liveness health check. No auth required.
